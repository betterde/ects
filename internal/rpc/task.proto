syntax = "proto3";

package rpc;

service Task {
    rpc Run(Request) returns (Response) {}
}

message Request {
    string command = 2; // 命令
    int32 timeout = 3;  // 任务执行超时时间
    string id = 4; // 执行任务唯一ID
}

message Response {
    string output = 1; // 命令标准输出
    string error = 2;  // 命令错误
}